services:
  brandstof-scout:
    container_name: fuel-scout
    restart: unless-stopped
    env_file:
      - ./.env
    build:
      context: .
    volumes:
      - /etc/localtime:/etc/localtime:ro
      # - ./volumes/plots:/app/plots
    ports:
      - 8000:8000

